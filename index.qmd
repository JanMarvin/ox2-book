# Preface {.unnumbered}

```{r setup_r, echo=FALSE, cache=TRUE, message=FALSE}
# setup book r environment
install.packages(
  'openxlsx2',
  repos = c('https://janmarvin.r-universe.dev',
            'https://cloud.r-project.org')
)
```


This is a work in progress book describing the features of `openxlsx2` [@openxlsx2]. Having written a book before, I never imagined to do this again and therefore I shall not do it. But still I consider it a nice addition to have something more flexible as our `vignettes`.